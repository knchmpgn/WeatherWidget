<Window x:Class="WeatherWidget.Views.TaskbarWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Weather"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        SizeToContent="WidthAndHeight"
        MinWidth="120"
        MaxWidth="180"
        Height="40"
        WindowStartupLocation="Manual"
        MouseLeftButtonDown="Widget_Click"
        MouseRightButtonDown="Widget_RightClick"
        MouseEnter="Widget_MouseEnter"
        MouseLeave="Widget_MouseLeave"
        Loaded="Window_Loaded">

    <Border x:Name="MainBorder"
            Background="Transparent"
            CornerRadius="6"
            Padding="10,4"
            Cursor="Hand">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Image x:Name="WeatherIcon"
                   Width="30"
                   Height="30"
                   Margin="0,0,8,0"
                   RenderOptions.BitmapScalingMode="HighQuality" />
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="TempText"
                           Text="--°"
                           FontSize="11"
                           FontWeight="Bold"
                           Foreground="White"
                           FontFamily="Segoe UI Variable Display"
                           Margin="0,-2,0,0" />
                <TextBlock x:Name="ConditionText"
                           Text="Loading..."
                           FontSize="10.25"
                           Foreground="White"
                           Opacity="0.8"
                           FontFamily="Segoe UI Variable Display Small"
                           TextTrimming="CharacterEllipsis"
                           MaxWidth="100" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>